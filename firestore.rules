rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --------- HELPERS DE USUARIO / ROLES ---------

    function isSignedIn() {
      return request.auth != null;
    }

    function userDocPath() {
      return /databases/$(database)/documents/users/$(request.auth.uid);
    }

    function userExists() {
      return isSignedIn() && exists(userDocPath());
    }

    function userRole() {
      return userExists() ? get(userDocPath()).data.role : null;
    }

    function isAdmin() {
      return userRole() == "admin";
    }

    function isRecepcion() {
      return userRole() == "recepcion";
    }

    function isCocina() {
      return userRole() == "cocina";
    }

    function isStaff() {
      return isAdmin() || isRecepcion() || isCocina();
    }

    // mismos strings que el frontend (STATUS)
    function isValidOrderStatus(status) {
      return status in ["nuevo", "proceso", "listo", "despachado"];
    }

    // --------- USERS ---------
    match /users/{userId} {
      allow read: if isSignedIn() && request.auth.uid == userId;
      allow read, write: if isAdmin();
    }

    // --------- ORDERS ---------
    match /orders/{orderId} {
      // doc principal de la orden
      allow read: if isStaff();

      allow create: if (isRecepcion() || isAdmin())
        && request.resource.data.total is number
        && request.resource.data.total > 0
        && request.resource.data.items is list
        && request.resource.data.items.size() > 0
        && request.resource.data.status is string
        && isValidOrderStatus(request.resource.data.status);

      allow update: if isCocina()
        && request.resource.data.status is string
        && isValidOrderStatus(request.resource.data.status)
        && request.resource.data.diff(resource.data).changedKeys().hasOnly(["status"]);

      allow update: if isRecepcion()
        && resource.data.status == "nuevo"
        && request.resource.data.status is string
        && isValidOrderStatus(request.resource.data.status)
        && request.resource.data.diff(resource.data).changedKeys().hasOnly(["status"]);

      // admin puede hacer de todo sobre la orden
      allow create, read, update, delete: if isAdmin();

      // --------- SUBCOLECCIÓN: items de la orden ---------
      match /items/{itemId} {
        // cocina / recepción / admin pueden ver los items
        allow read: if isStaff();

        // recepción crea los items cuando manda la orden; admin también puede
        allow create, update, delete: if isRecepcion() || isAdmin();
      }
    }

    // --------- ARCHIVED ORDERS ---------
    match /archived_orders/{orderId} {
      allow read, write: if isAdmin();
    }

    // --------- MENU ITEMS ---------
    match /menuItems/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // --------- CONFIGURACIÓN GLOBAL ---------
    match /configuration/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // --------- STATS DIARIAS ---------
    match /daily_stats/{id} {
      allow read: if isAdmin();
      // saveOrder (recepción) escribe aquí
      allow write: if isRecepcion() || isAdmin();
    }

    // --------- COUNTERS ---------
    match /counters/{id} {
      allow read: if isStaff();
      // generateOrderNumber (recepción) actualiza el contador
      allow write: if isRecepcion() || isAdmin();
    }

    // --------- INVENTARIO DETALLADO ---------
    match /ingredients/{id} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }
    match /sides/{id} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }
    match /drinks/{id} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }
    match /potatoes/{id} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }
    match /sauces/{id} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }
    match /combos/{id} {
      allow read: if isStaff();
      allow write: if isAdmin();
    }
  }
}
