rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERMITIR TODO SI ESTÁ LOGUEADO
      // Esto soluciona el error "Missing or insufficient permissions"
      allow read, write: if request.auth != null;
    }
  }
}